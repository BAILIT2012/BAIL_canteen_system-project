const maintenanceConfig = {
  enabled: false,       // true kar doge → pura app maintenance mode me chala jayega
  message: "We are updating the system. New version coming soon!",
};

export default maintenanceConfig;
